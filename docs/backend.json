
{
  "entities": {
    "Table": {
      "title": "Table",
      "type": "object",
      "description": "Represents a single poker game table.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the table."
        },
        "createdAt": {
          "type": "object",
          "description": "The timestamp when the table was created."
        }
      },
      "required": ["name", "createdAt"]
    },
    "Player": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Player",
      "type": "object",
      "description": "Represents a player in the poker game.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The player's chosen name."
        },
        "blackCoins": {
          "type": "number",
          "description": "The number of black coins the player has at the end of the game."
        },
        "createdAt": {
          "type": "object",
          "description": "The timestamp when the player was created."
        },
        "rebuyTimestamps": {
          "type": "array",
          "description": "An array of timestamps for each buy-in and re-buy.",
          "items": {
            "type": "object"
          }
        },
        "hasPendingRebuyRequest": {
          "type": "boolean",
          "description": "True if the player has requested a rebuy and is waiting for dealer approval."
        }
      },
      "required": [
        "name",
        "blackCoins",
        "createdAt",
        "rebuyTimestamps",
        "hasPendingRebuyRequest"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/tables/{tableId}",
        "definition": {
          "entityName": "Table",
          "schema": {
            "$ref": "#/backend/entities/Table"
          },
          "description": "Stores information for each poker table."
        }
      },
      {
        "path": "/tables/{tableId}/players/{playerId}",
        "definition": {
          "entityName": "Player",
          "schema": {
            "$ref": "#/backend/entities/Player"
          },
          "description": "Stores information for each player at a specific table."
        }
      }
    ]
  }
}
