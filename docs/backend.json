
{
  "entities": {
    "Player": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Player",
      "type": "object",
      "description": "Represents a player in the poker game.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The player's chosen name."
        },
        "blackCoins": {
          "type": "number",
          "description": "The number of black coins the player has at the end of the game."
        },
        "createdAt": {
          "type": "object",
          "description": "The timestamp when the player was created."
        },
        "rebuyTimestamps": {
          "type": "array",
          "description": "An array of timestamps for each buy-in and re-buy.",
          "items": {
            "type": "object"
          }
        },
        "hasPendingRebuyRequest": {
          "type": "boolean",
          "description": "True if the player has requested a rebuy and is waiting for dealer approval."
        }
      },
      "required": [
        "name",
        "blackCoins",
        "createdAt",
        "rebuyTimestamps",
        "hasPendingRebuyRequest"
      ]
    },
    "Log": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Log",
      "type": "object",
      "description": "Represents a log entry for a game event.",
      "properties": {
        "message": {
          "type": "string",
          "description": "The description of the event."
        },
        "createdAt": {
          "type": "object",
          "description": "The timestamp when the event occurred."
        }
      },
      "required": [
        "message",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/players/{playerId}",
        "definition": {
          "entityName": "Player",
          "schema": {
            "$ref": "#/backend/entities/Player"
          },
          "description": "Stores information for each player in the game.",
          "params": [
            {
              "name": "playerId",
              "description": "The unique ID of the player document."
            }
          ]
        }
      },
      {
        "path": "/logs/{logId}",
        "definition": {
          "entityName": "Log",
          "schema": {
            "$ref": "#/backend/entities/Log"
          },
          "description": "Stores a log of all game actions.",
          "params": [
            {
              "name": "logId",
              "description": "The unique ID of the log document."
            }
          ]
        }
      }
    ]
  }
}
